/**
 * @module components
 * Contains reusable stateless components. All components should either be global
 * to the app or completely reusable
 */
import Header from './Header/Header';

export {
  Header as default,
};
